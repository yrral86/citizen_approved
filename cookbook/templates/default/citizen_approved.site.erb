server {
  listen   80;
  server_name  <%= @server_name %>;
  root <%= @docroot %>;

  passenger_enabled on;

  access_log  /var/log/nginx/citizen_approved.access.log;

  location / {
    root   /var/www/nginx-default;
    index  index.html index.htm;
  }
}
